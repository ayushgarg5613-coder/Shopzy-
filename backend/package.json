{
  "name": "shopzy-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd .. && tsx backend/server.ts",
    "build": "esbuild server.ts --bundle --platform=node --format=cjs --packages=external --sourcemap --outfile=../dist/server.cjs",
    "start": "NODE_ENV=production node ../dist/server.cjs"
  }
}
